-- Copy and paste this directly into your Supabase SQL Editor to fix the missing tables:

CREATE TABLE IF NOT EXISTS public.tiktok_recommendations (
    id TEXT PRIMARY KEY,
    product_name TEXT NOT NULL,
    display_headline TEXT NOT NULL,
    visual_badge_text TEXT NOT NULL,
    destination_url TEXT NOT NULL,
    is_active BOOLEAN DEFAULT false
);

CREATE TABLE IF NOT EXISTS public.query_cache (
    id UUID DEFAULT uuid_generate_v4() PRIMARY KEY,
    prompt TEXT UNIQUE NOT NULL,
    response TEXT NOT NULL,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT timezone('utc'::text, now()) NOT NULL
);

-- Insert dummy data for TikTok to test
INSERT INTO public.tiktok_recommendations (id, product_name, display_headline, visual_badge_text, destination_url, is_active)
VALUES ('default_desk_mat', 'AI Developer Desk Mat', 'Upgrade your workspace with this premium shortcut mat.', 'HOT TRENDING 🔥', 'https://www.tiktok.com', true)
ON CONFLICT (id) DO NOTHING;
